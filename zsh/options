#setopt null_glob # silently ignore non-matching globs, applies all files when none matches (careful with rm !)

# MORE
[ -f $ZSH_PRIVATE/_options ] && source $ZSH_PRIVATE/_options

# vim: set ft=zsh:
