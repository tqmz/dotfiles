" SYNTASTIC CONFIGURATION
" :h syntastic

" PHP
" :h syntastic-filetype-checkers
" :SyntasticInfo  (when current buffer is filetype php)
let g:syntastic_php_checkers = ["php"] " phpcs, phpmd left out here
