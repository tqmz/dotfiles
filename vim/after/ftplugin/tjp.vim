" Compile project via Enter in Normal Mode
noremap <C-M> :!tj3 %<CR>

" Use t3jman for Taskjuggler manpages (`k` over word)
set keywordprg=tj3man

" Apply example files indent width of 2 chars
set expandtab " do use spaces instead of tabs
set shiftwidth=2
set softtabstop=2
set tabstop=2
